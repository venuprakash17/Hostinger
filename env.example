# Environment Variables for Local Development (Matches VPS Production)
# Copy this to .env and fill in your values

# PostgreSQL Database Password (for Docker Compose)
# This matches the VPS production setup
POSTGRES_PASSWORD=dev_password_123

# Backend Secret Key
# Generate with: openssl rand -hex 32
SECRET_KEY=dev-secret-key-change-in-production

# CORS Origins (comma-separated, no spaces)
# For local development
BACKEND_CORS_ORIGINS=http://localhost:8080,http://localhost:5173,http://127.0.0.1:8080,http://127.0.0.1:5173

# Frontend API Base URL
# This is used when building the frontend
VITE_API_BASE_URL=http://localhost:8000/api/v1

# OpenAI API Key for ResumeItNow Integration
# Get your API key from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=your-openai-api-key-here
