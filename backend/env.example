# Database Configuration
# ======================

# DEVELOPMENT: SQLite (Free, File-based, No Setup Required)
# Default: Uses SQLite database file (elevate_edu.db)
# No changes needed - works out of the box!
DATABASE_URL=sqlite:///./elevate_edu.db

# PRODUCTION: PostgreSQL (For Docker/VPS Deployment)
# Uncomment and configure when deploying with Docker:
# DATABASE_URL=postgresql://elevate_user:password@postgres:5432/elevate_edu

# Security
# ========
# Generate a secret key: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-change-this-in-production

# API Settings
# ============
API_V1_STR=/api/v1

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:8080,http://localhost:5173

# Token Expiration
ACCESS_TOKEN_EXPIRE_MINUTES=10080  # 7 days
REFRESH_TOKEN_EXPIRE_DAYS=30

# Production Settings (for Docker/VPS)
# ====================================
# POSTGRES_PASSWORD=your_secure_password_here
